<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="<%= css %>">
</head>
<body>
    <nav>
        <a href="/">🏠 Inicio</a>
        <a href="/productos">📦 Productos</a>
        <a href="/contacto">📧 Contacto</a>
    </nav>

    <h1><%= titulo %></h1>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Producto</th>
                <th>Precio</th>
            </tr>
        </thead>
        <tbody>
            <% datos.productos.forEach(function(p) { %>
                <tr>
                    <td><%= p.id %></td>
                    <td><%= p.nombre %></td>
                    <td>$<%= p.precio.toLocaleString() %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <p class="nota">
        Esta vista fue despachada por el <strong>Dispatcher</strong> desde la ruta <code>/productos</code>
    </p>
</body>
</html>
